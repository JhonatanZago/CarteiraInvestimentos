import { Component, inject } from '@angular/core'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { RouterLink } from '@angular/router'; import { HttpClient } from '@angular/common/http'; import { API_BASE_URL } from '../../core/api/api-url.token';
@Component({selector:'app-forgot-password-page',standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<main class="auth-simple"><section><h1>Esqueci minha senha</h1><p>Informe seu e-mail para receber as instruções.</p><form [formGroup]="form" (ngSubmit)="submit()"><input type="email" formControlName="email" placeholder="Seu e-mail"><button [disabled]="form.invalid">Enviar instruções</button></form>@if(sent){<p>Se o e-mail estiver cadastrado, você receberá as instruções para redefinir sua senha.</p>}<a routerLink="/login">Voltar ao login</a></section></main>`,styles:[`.auth-simple{min-height:100vh;display:grid;place-items:center;background:var(--background);padding:1rem}.auth-simple section{width:min(32rem,100%);padding:2rem;background:var(--surface);border:1px solid var(--border);border-radius:1rem}.auth-simple form{display:grid;gap:.8rem}`]})
export class ForgotPasswordPage { private readonly fb=inject(FormBuilder); private readonly http=inject(HttpClient); private readonly base=inject(API_BASE_URL); readonly form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]]}); sent=false; submit(){if(this.form.invalid)return;this.http.post(`${this.base}/auth/esqueci-minha-senha`,this.form.getRawValue()).subscribe(()=>this.sent=true);}}
